What I have done so far: 

I extracted the plots and model run from the new Cython script from the test run and put them in an extra function. 
Furthermore, I included our functions for calculating the NSE and the objective function. 
Then, in the def main() from row 283, I started with creating A, B 
with i (columns) = number of model parameters and rows N >=500 (as recommended in the paper and by Faizan). For the Sobol sampling, I used a scipy function: https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.qmc.Sobol.html

(N = 2**m -> so I chosen m= 9 resulting in N = 512) 

After that, I created the C(i)s as recommended in the paper, with A_B(i) -> so I used A as the base and then replaced the i-th column with B.


Next step: 
implemtne f(A), f(B),f(A_B(i))

and after that the S_i and S-Ti (+everything needed for that) 
